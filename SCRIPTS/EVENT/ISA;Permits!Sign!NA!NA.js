//ISA;Permits!Sign!NA!NA

// added by Vance Smith (Mesa)
//include("PMT_AssignInspector");

//test to be removed
if (inspType == "Footing") {
	logDebug("one inspection " + inspType);
}
if (inspType == "Misc Inspection") {
	logDebug("two inspection " + inspType);
}